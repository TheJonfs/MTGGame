# decisions.md — APPEND (Session 37)

*Planner-authored appends, implementer-filed per the S37 brief (Part 0); home is `docs/decision-updates/`. Add after ADR-121.*

---

**ADR-122 — Phase two's design is ratified as `phase-two-design-draft-1.md`.** The Flood; one journey; the five lord/court pairs under the five laws; triad gates at strongholds, shape gates at courts; the accumulating Heart; the salvage start. Its open list stands; its engineering sequence begins with the deckbuilder.

**ADR-123 — Saved decks and door rules.** A save holds several named decks over one collection; an opponent template may carry a `deckRule`; the deck editor and the parley both validate against it. No phase-two content, no rule on any existing template this session.

**Placement**: Pell of the Shallows −1 Thawing Glaciers +1 Island (the S36 read: −7 against the stock starters; the Glaciers stays in Quill and the shop).

*Implementer's filing notes:* the "several decks per save" of ADR-123 has been in the save since S16 (v3: `world.decks` keyed by name, `activeDeckName`; new / rename / duplicate / delete / switch in the editor) — Chris ruled (2026-09-15) to keep that record shape rather than the brief's array-with-ids, so the save format stays `world-save-v7` and no migration ships. What S37 adds on top: `checkDeck` (every problem as a sentence, ownership folded in, an optional `DeckRule`), the editor's legality panel and average mana value, the door refusal through the parley, `deckRule` on `OpponentTemplate` (validated, rendered in enemies.md, none authored), and the single-game picker's saved decks behind the gallery's unlock rule.
