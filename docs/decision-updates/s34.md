# decisions.md — APPEND (Session 34)

*Planner-authored appends, implementer-filed per the S34 brief (draft, Part 0); home is `docs/decision-updates/`. Add after ADR-115.*

---

**ADR-116 — Two yardsticks, one ladder.** The tier tables are read against both: the mid-road references (a starter plus eight shop cards, one basic in play, 12 life, journeyman) for tiers 2–3, and the stock starters for tier 1 and as the "walked in early" bound at tiers 2–3. Standard is the cell where a mid-road journeyman wins 55–65% and a stock starter is unfavourable but winnable (roughly 25–40%). Easy is one cell easier, Hard one harder, on the entrance axis first (the strong lever), life second.

**ADR-117 — The tier tables (⚠ cells for ratification, Part 2).** Life / basics in play; beasts a life delta on the catalog row, no entrance. Mages T1 8/0 at every mode; T2 easy 12/0, **standard 12/1**, hard 14/2; T3 easy 14/1, **standard 16/2**, hard 20/2. Beasts T1 +0; T2 easy +0, **standard +2**, hard +4; T3 easy +2, **standard +4**, hard +8. Per-row offsets on the catalog row (`worldLifeOffset`, applied after the tier delta): the Serra Angel −4; nothing else this session.

**ADR-118 — The white and blue roads are not tuned off the sim.** Dawn Levy overperforms and Tidal Grimoire underperforms in every table by about the same margin, and Chris's read of the cause is right: the heuristic AI executes a weenie curve well and plays instants and counters badly. The human pilots the starter; the sim's blue numbers understate what a person does with Tidal Grimoire, and the white numbers overstate a curve the player will have to leave behind at the first boss (ADR-114). Both roads stand; their felt difficulty is read from Chris's own play, not the roster.

**The S28 window's second erratum.** "In response" is true only over an OPPONENT's item on the stack — fixed in both gates (the cantrip window, the flash creature), ladder run. Recorded as the second erratum to S28 (the first: the step's name, S32).

**The Lab** is the exploratory surface; the ledger records ratified cells from a named saved run (`analysis/runs/<name>.json`), never from an unsaved grid.

*Implementer's filing notes:* the tables are encoded as the knobs `mageTierLife`, `mageTierEntrance`, `beastTierLifeDelta` with the drafted Standard values as defaults and the easy/hard cells in the bundles — **⚠ provisional until the cells are ratified** (the T1 cell: Chris ran 6, the planner prefers 8 — 8 is encoded, the current value). The resolver takes the mode through the knobs (`resolveMatchup(opponent, knobs, legacy)`) rather than a separate `mode` argument — every caller already holds `worldKnobs(world)`, which carries the bundle. Part 4's list trims (Oriel, Vael) are **measured, not applied** — the brief marks them ⚠ Chris, and this session leans on analysis before list changes; the numbers are in the handoff.
