# Handoff — after Session 23 (2026-08-27)

## State of the world

**The fun batch is in and fuzzed-first, the wilds have rivers and faces, the dungeons lost their grid, and Cinquefoil made its first sound.** Pool 171 → **174**: Thundersnake ({R}{R} 4/1 trample haste, end-step self-sacrifice), Gallows Djinn ({2}{B}{B} 5/5, the aggression tax — attack or block, pay 1 to its own keeper), Traumatizer ({2}{U}{U} 2/4 flying, combat damage to a player mills twice that many) — all tier 2, all custom, all riding two tiny words (the self-`sacrifice` effect surfaced from A10 word 3's path; the `eventDamage` value ref with ADR-028 reaffirmed) plus two first-collector skeleton wirings (END_STEP, BLOCKS — the R-061 precedent; both combat collectors now carry eventContext, so the Djinn's tax composes from the Warden's event addressing with zero new words). The world grew RIVERS (2–4 seeded meandering ribbons, impassable except road bridges and natural fords; reachability repairs by promoting the blocking wet cell to a ford — the ribbon never breaks), town FOOTPRINTS by ring (walled sprawl / hamlet / palisade huddle through the sprite pipeline, two first-try renders), and the interior register's SMOOTHING pass (carved walls chained + corner-cut + guttered — no more grid staircase; both snapshot-ledgered). Audio scaffolding shipped CUE-FIRST per ADR-083/084: an 11-cue taxonomy, an AudioManager with crossfade/stingers/persisted toggle (front page + in-game chrome), mapping.json against a gitignored `assets/audio/` mount served at `/audio`, silent-if-unmapped as the deploy's natural state — smoke-verified end to end with placeholder tones (menu cue post-interaction, context switch, mute persisting across reload), then the mapping reverted to committed-empty. The 12 card-art candidates are rendered (zero refusals) and await Chris's verdicts. Part 0 ceremonies: ADR-083 (collected mid-kickoff) + ADR-084 applied; handoff-s22.md synced with playtest rounds 3–4; the empowerment TIERS three-place sync folded onto the knob (guardian-sim and lord-sim moved into packages/world and read `dungeonEmpowermentTiers`/`strongholdEmpowermentTiers` through dungeon.ts's one builder).

## Done this session

- **Part 0**: ADR-083/084 appended to decisions.md (083 arrived mid-kickoff via Chris's drop-in; the planner's manalink-pointer rewording applied to quests.json, asserted); handoff-s22.md ← handoff.md sync (the numbered archive now carries r3–r4); the TIERS fold — `guardian-sim-cli.ts`/`lord-sim-cli.ts` moved sim → world (imports via the sim subpath exports; scripts repointed), schedules read the knobs, packages come from `empowermentModifiers` (both smoke-run at 1 game/cell from the new home); FUZZ_FULL baseline 375/375.
- **Part 1 (the fun batch)**: kickoff corrections Chris-ruled (Thundersnake prices by formula — 18g, the spec's 12 was Bolt's price riding along; first collectors counted as skeleton, not bill; the Djinn art seed = the bargain, Juzam homage welcome; snake AI gate = own MAIN1 + the cheap big-blocker check now; Gallows Djinn named). Vocabulary: `sacrifice` (scope self, validator-confined, resolves through the one primitive — DIES fires, no-op if the source left) + `{ref: "eventDamage", times: N}` (mill.count widened to refs; validator confines the ref to damage-event triggers; eventContext gains `amount`). Engine: END_STEP collector (STEP_BEGIN(END), every end step by default, controller condition for future customers) + BLOCKS collector (self-condition, declaration order) + eventContext on ATTACKS/BLOCKS. Three defs with `text`. **Fuzz before fixtures**: 300 games over batch decks built to stumble into every line (Zombify reanimates the snake off-turn, Rancor tramples the Traumatizer), with a COVERAGE PROBE asserting each card's machinery fired; replay byte-identical through the new triggers. **9 fixtures** incl. the spec's named three: off-turn snake dies same turn; the Djinn at life 1 kills its keeper (result {winner:1, LIFE}); the trample partial — 1 assigned mills 2 (plus: bounce-under-the-trigger no-op; block tax with the 5/5 eating the attacker; walled-mills-nothing). **AI**: `fleetingWasteGated` (own MAIN1 only + never into an untapped toughness ≥ power defender), the Djinn's lethal-tax pins (never attack/block when the tax kills the keeper — offered-list and greedyBlocks filters) + tax priced into attack sets and block gains at the archetype's own-life rate. Book of shame 23–24. **Ladder 1,000/cell: gates PASS, zero regression.** Registry: R-084; pool-registry S23 section; loader pin 174.
- **Part 2 (wilds polish)**: RIVERS — `riversPerWorld`/`riverFordsPerRiver` knobs, `WorldMap.river`/`ford` optional fields (pre-S23 maps unaffected — maps live in saves), generation as step 6b (after roads: crossings become bridges; before spawn: reachability honest), forward-biased meander with lateral runs capped at 2 (the first walk's 44-cell flats read as lakes — browser-caught), the extended invariant (fordTo: plan through water, promote wet cells; rough-only blockage still carves), natural fords clear their cell. Renderer: chained/smoothed water (wide wash + dark line) under the roads, bridge rails at river∧road, stepping-stones at fords, minimap tint; 200-seed invariant fuzz green with rivers in; the water-law + determinism test and the BRIDGED-ROAD-WALKED test (every wet cell on every town path is a crossing; crossings exist across seeds). FOOTPRINTS — `sprite-town-city` (walled sprawl) + `sprite-town-huddle` (palisade) rendered first-try, processed through the S21 pipeline (levels-to-white, ink-bbox crop, bottom-anchor 256px), picked by ring with sizes 3.7/3/2.4 CELL; three registers verified in one browser view. SMOOTHING — interior wall edges through chain+Chaikin+wobble with a dark gutter under the pale chisel line; snapshots `wilds-rivers-bridge-s23.png` + `interior-smoothing-s23.png` ledgered.
- **Part 3 (audio, cue-first)**: the 11-cue taxonomy as proposed (no balloon); `packages/ui/src/audio/audio.ts` (AudioManager: crossfade music, fire-and-forget stingers, persisted enable, autoplay unlock on first gesture, silent no-op on unmapped/unmounted, test seams); `data/audio/mapping.json` committed EMPTY (underscore keys as docs); gitignored `assets/audio/` mount + `/audio` dev middleware; wiring — screen-driven music (menu/overworld/town/dungeon/stronghold/duel), stingers (quest-complete vs siege-news by popup title, duel win/loss on duelResult, coin-flip on the ceremony); the prominent front-page toggle + the in-game chrome mute (Chris's kickoff addition), one persisted preference. 4 unit tests (empty-mapping-total-no-op, mount resolution, headless never-throws, subscriber mirror). Browser smoke with generated tones: menu plays after first click, context switch fetches on cue, mute kills + persists across reload; mapping reverted after.
- **Part 4**: scripted acceptance green (batch fixtures, the bridged road, audio smoke); snapshots ledgered; 12 card-art candidates + 2 sprites in MANIFEST (candidates vs kept).

## Deviations from the brief

1. **Kickoff corrections recorded inside ADR-084's applied text** (implementer note in place): the 18g formula price; first collectors as skeleton. Chris ruled both live.
2. **The Thundersnake's big-blocker check shipped now** (Chris's "if it's cheap" — it was one `some()`): gate when any untapped enemy creature has toughness ≥ the snake's power. Deliberately ignores deathtouch-under-toughness and flying-only boards.
3. **The audio singleton is module-level** (`export const audio`); dev HMR can double-evaluate the module (two instances observed in the network log). Harmless for idempotent cue-setting; flagged rather than engineered around.
4. **Repair fords can chain lengthwise** when the reachability path runs ALONG a river before crossing (a 4-cell "causeway" at seed 42). Legal, reachable, looks like a submerged road; a perpendicular-crossing preference is one heuristic away if the feel is wrong.
5. **The wild-town huddle sprite includes a leaning watch-post** not in the brief's two-word spec ("wild towns huddle") — taken as descriptor license; re-render is one command if it reads wrong.

## Concerns

1. **River sources cluster by seed luck** — seed 42's 2–3 rivers all run in the map's top band (y 0–22); the southern half is dry. If a "every world feels riverine" want emerges, source placement should stratify (one per map third, or spoke-anchored). Arguing baseline shipped as-is.
2. **The Djinn's tax pricing uses the archetype's ownLife weight flat** (0.1–0.25/point) — at 10-life world starts a 1-point tax is ~10% of a life total and arguably worth more near the floor. The hard never-lethal pins hold the felt-wrong line; a life-scaled tax price is tuning territory (ladder showed zero regression at the flat rate).
3. **music.stronghold vs music.dungeon inside a STRONGHOLD'S interior duel is not distinguished** — dungeonDuel screens all play music.duel, and the interior walk picks stronghold/dungeon by run kind; but the telegraph→interior handoff briefly passes through overworld on decline paths. Cosmetic at worst; the mapping round will hear it.
4. ~~RESOLVED same session — the verdict round ran twice~~: Chris rejected round 1 WHOLE ("nothing really speaks to me") and called a fresh roll in four styles NEW to the project — stained glass, illuminated manuscript, art nouveau poster, dark surrealist oil (card-art.md round-2 section locks the language). Round 2's twelve rendered zero-refusal; picks: Thundersnake STAINED GLASS, Gallows Djinn DARK OIL, Traumatizer ART NOUVEAU — cropped 5:4, art.asset wired, gallery-verified; all 24 verdicts in MANIFEST. Printed JPGs arrived same session (assets/temp/Cinquefoil Assets/) and are WIRED: 745px resample, printedAsset on all three defs, MANIFEST provenance rows, printed-toggle gallery-verified. ONE TYPO on the Traumatizer's printed face — "that players mills" — flagged to Chris (a corrected re-drop re-runs the idempotent pipeline). **Planner note:** the round-2 styles are the first non-house-register card art in the pool; if one becomes a recurring register, the ADR-052 style list may want formal expansion.
5. **The audio unlock listens for pointerdown/keydown once** — if the first-ever gesture happens while the toggle is OFF, unlock still occurs (correct); but a user whose FIRST gesture is the mute click will hear one crossfade frame race the pause. Unobserved in smoke; noted for the mapping round's ears.
6. **lord-sim's LIVES table still hand-carries the pace-war sample points** (15/30/50) — the TIERS mirror folded, but the life postures are analysis constants, not knobs. Left deliberately: they're the SIM'S question, not the game's schedule.

## Registry entries added/changed

R-084 (END_STEP + BLOCKS collectors, self-sacrifice, eventDamage — the fun batch's engine pieces). Pool-registry: S23 section (3 rows), tier tally T2 41→44, loader pin 171→174. decisions.md: ADR-083 (collected mid-kickoff) + ADR-084 applied with kickoff-correction notes in place. Knobs: `riversPerWorld`, `riverFordsPerRiver` (docs/knobs.md regenerated). New: `data/audio/mapping.json` (committed, empty), `packages/ui/src/audio/` (manager + tests), `/audio` + unchanged `/[...]` dev middlewares, `assets/audio/` gitignored; `WorldMap.river`/`ford` optional fields; guardian-sim/lord-sim now `packages/world/src/*-cli.ts`; card-art.md entries ×3 + 14 subject files (12 candidates, 2 sprites); MANIFEST 14 rows; snapshots ×2; book of shame 23–24; implementer-notes S23 lessons.

## Test status

**FUZZ_FULL 394/394** (S22-r4's 375 → +18: the 300-game batch fuzz with coverage probe + replay, 9 batch fixtures, book of shame 23–24, 4 audio units, rivers water-law/determinism + the bridged-road walk, the tier-tally re-pin). **Ladder 1,000/cell after the batch AI: gates PASS, zero regression** (cells 99.8–100%, all mirrors majority). Typecheck + Babel parse gate clean. 200-seed generator invariant fuzz green WITH rivers. Browser-verified: rivers/bridges/fords live at seed 42 (+ snapshot), three town footprints in one view, the smoothed interior (+ snapshot), the full audio smoke (play/switch/mute/persist), the front-page toggle + chrome mute. guardian-sim + lord-sim smoke-run from their new home. 12/12 card candidates rendered, zero refusals.

## Suggested next

1. **Chris's round**: ~~verdicts~~ done (see concern 4 — the fresh-roll story); cast the three cards in anger; cross a ford; find a wild huddle; hear the smoke tone or start the mapping round; feel-check the rivers (concern 1's clustering) and the causeway fords (deviation 4).
2. **The audio mapping round** (step 2): Chris + planner author cue→file against the Shandalar library — the scaffolding is waiting; concern 3's stronghold-duel cue question rides it.
3. **Planner ratifications**: the kickoff corrections as recorded in ADR-084's applied text; deviation 2's blocker-check shape; the river baselines (2–4 rivers, 1–2 fords, meander constants) and concern 1 if stratified sources are wanted.
4. **Parallel round (per ADR-084)**: Chris's standard-mode stronghold sprint → lord-deck iteration + watch-flag rulings; the travel-powers design round after.
5. **S24 candidates**: the verdict round's crops + printedAsset wiring on receipt; Unearth (bill essentially paid — returnFromGraveyard + an mv-at-most predicate + cycling); the R-economy; wild towns ≥2/world guarantee if the wilds-polish want returns.

## How to run

```
pnpm test / FUZZ_FULL=1 pnpm test
pnpm ladder                                # 1,000/cell gates
pnpm guardian-sim --games 15               # now packages/world/src/guardian-sim-cli.ts (knob-fed)
pnpm lord-sim --games 10                   # now packages/world/src/lord-sim-cli.ts (knob-fed)
pnpm viewer → /world (rivers on any NEW world; the fun cards in shops at tier 2; the sound toggle on the title card)
# audio smoke: drop any .wav/.ogg/.mp3/.flac into assets/audio/, map it in data/audio/mapping.json, restart the dev server
pnpm knobs:doc                             # after knob edits
```
