# decisions.md — ADR-072 append (mid-S16; APPEND, do not replace)

**ADR-072 — The radial world (ratifies the S16 mid-session proposal §2–§5, with three additions).** Five seed-jittered colour spokes × three rings from an elliptically-normalised centre; 15 region hearts into the existing L1-Voronoi; strongholds as fixed points at r≈0.92 per spoke (present, unused until S19–S21); towns per region by ring-tiered knobs with the every-colour-has-a-civilized-town invariant; `mapScale` with counts × mapScale² and distances × mapScale, **sight radius absolute**; roads as flagged shortest paths between neighbour towns with terrain-keyed roamer speed (`roamerStepsPerPlayerStep`, fractional-accumulating — future terrains and boots-class effects are keys/modifiers on this knob); region-bound roamers, parley-removes-roamer, per-area density, ring-tiered respawn (**the project's first clock consumer — noted**). Generator fuzz invariants per the proposal.

*Planner additions:* (1) `explored` **reserved in `world-save-v3`** (packed bits; migration defaults all-explored); the fog system itself is deferred — when built: unexplored renders blank parchment *including washes*, POI fixed-points spawn into view on first sight, and **the player's home region starts explored**. (2) Proposal-§7.4 defaults adopted as the arguing baseline; world-sim reports them. (3) Catalog content per `docs/world-catalog-content.md` (this drop) — encode into `data/world/regions.json` (catalog v0→v1); placeholder names are not needed.

*World name:* planner proposes **Cinquefoil** (five-petaled flower, heraldic device — and retroactively the meaning of the S6 card back's five-petal compass rose). Chris to ratify or rename at leisure; no code depends on it yet.
