# Handoff — after Session 20 (2026-08-24)

## State of the world

**Dungeons and duals are live.** The **payment solver** (ADR-004 second amendment) replaced the per-colour count that silently passed Hall violations: `canPay`/`autoPay` run pip-to-producer matching (Kuhn's, deterministic, replay-stable), duals tap for their assigned colour, deliberate taps enumerate per colour, pre-S20 logs replay unchanged. **A9 `entersChoice`** (the shock clause) asks on the land play only — pay 2 → untapped, else tapped; paying to exactly 0 is legal and lethal; every put-path enters tapped choice-free. **A8's "up to" targeting finally shipped** with Drakuseth (range counts, last-spec rule, no-stacking distinctness, `targetSpec` fan-out with per-target fizzle) alongside the vocabulary's first **ATTACKS collector**. **The land-and-legend batch**: 10 ABU duals (R, OLGC earlier-of-two, "priceless"), 10 RVR retro shocklands (T2, `priceOverride` 45g), 5 cycling lands + Evolving Wilds (T1, 10/12g), Reya/Arcanis/Drakuseth/Titania (R), 5 Moxen (prizeOnly, LEA), the 5/3 Elemental token — **pool 147**, every printing override verified. **Nonbasic lands stock now** (either-colour rule). **The dungeon system** (the milestone): mini-worlds on the existing map stack — fogged branching interiors, chokepoint minions (no parley), treasures to **escrow**, the exterior clock frozen while an **interior step counter** drives visible **empowerment tiers**, an **interior life track** carried duel to duel, walk-out/defeat **reset + forfeit**, victory pays escrow + prize; **reload resumes mid-dungeon**. Five authored **Mox dungeons** (laws live on both sides of every interior duel) + the five tier-3 lairs converted to **lair-dungeons** (R-card treasures; the S14 certain-encounter is now a front door). **world-save-v5** (dungeons + reserved `strongholds: []`). **WBRUG spoke canon rode** (rotation/reflection jitter, adjacency invariant). Part 0: gates PASS at 1,000/cell; the **Nighthawk mod shipped** (Chris: leave as-is pending future tuning; 17% vs the 15% pre-mod baseline). **302/302 FUZZ_FULL**, typecheck clean.

## Done this session

- **Part 0:** ADR-079 appended (verified) + **R-057 backfilled**. Gate run 1,000/cell **PASS** — the Duress-fix ladder delta is noise (only D-mirrors moved, 75.2/79.0 → 75.3/78.8). Re-baseline on the current agent = the S19 tuned tables exactly (one black-seed trajectory changed — the fix is real but rare). **Nighthawk mod applied** (3× core, 4 Child, 3 Rats, 1 Doom Blade): 15% → **17%** — far under the 30–45% band; Chris ruled **ship as-is pending future tuning**. FUZZ_FULL from S19 was already green (not pending).
- **Part 1 — engine:** `solvePayment` (pips in WUBRG order; mono-first producer preference; generic from leftovers, {C} first; excludeProducers honoured); `tapForMana(objectId, symbol?)`; `tapForMana` action gains `color?`; enumerator emits one deliberate tap per colour. **A9**: `entersChoice` def field (validated: lands, shock shape), the `entersChoice` RequestPurpose on the land play, `loseLife` before the ETB state fixes, life ≥ cost gate, put-paths tapped via `moveObject`/`createObject` defaults (+ unconditional `entersTapped` for the cycling lands). **A8**: `count: {min,max}` + `distinctFromPrior` on TargetSpec (validator: range must be last), subset combos, walk-consume `validateTargetsAgainstSpecs`, `EffectContext.targetsOfSpec` + damage `targetSpec` fan-out, per-target fizzle. **ATTACKS** collected on ATTACKERS_DECLARED (self-condition; observed shapes await a customer). `returnFromGraveyard` gains `landCardInYourGraveyard`; `bounce` gains scope (Arcanis's self-return). **Fuzz before fixtures throughout**: `pnpm fuzz:duals` 800+600 (duals), 600+400 (`--shocks`), 1,500+900 (`--guardians`) — zero errors — then 20 fixtures across `s20-solver` / `s20-a9` / `s20-a8-legends` (the dual doc's named cases: both-ways assignment, the matching corner, the Hall case, determinism, per-colour taps, replay purity; pay/don't/life-2-lethal/life-1-no-ask/put-tapped; Drakuseth 0/2/fizzle, Titania's returns + Wilds→Elemental, Reya's raise, Arcanis's engine).
- **Part 2 — the batch (pool 147):** all defs Scryfall-verified with explicit printings (OLGC listing confirmed the planner's structure — 27 cards, year-coded numbers, earlier-of-two picks Scrubland 2018 / Volcanic 2018NA / Bayou 2019A; shocks = the RVR #397–415 retro run, Temple Garden #414 as ADR-079 said; Reya INV#33 / Arcanis ONS#66 / Drakuseth CMM#535 / Titania MH2#416 / Wilds BRC#184 all MATCH; Moxen defaulted to LEA Dan Frazier). `priceOverride` generic column (validated; `shopPrice` honours it under the global multiplier). **Bug caught by re-read: `shopPoolFor` excluded ALL lands** — shocks/enablers would never have stocked; now basics-only excluded, land identity = produced mana, **either-colour stocking** per ADR-079. Duals **unsellable** ("priceless" chips). Editor: nonbasic lands already 4-capped as ordinary cards (the S14 validation generalized); Land filter added. `fuzz-duals` CLI + `guardian-decks.ts` (§9 lists, 40 cards, Reya's Tactician swap) as sim exports.
- **Part 3 — dungeons:** `packages/world/src/dungeon.ts` — seeded topology (wiggling trunk → guardian, 2–4 treasure branches, one cross-link; minions spaced on the trunk from the spoke's T≤2 signatures; lair-dungeons smaller with **R-card treasure rolls**); `DungeonRun` wholly in **world-save-v5** (+ `dungeons` status map + reserved `strongholds: []` — the blessed field list); `dungeonAdvance` (interior steps, fog, escrow banking, minion/guardian stops); `dungeonDuelSpec` (interior life as startingLife; **the law on both seats of every interior duel**; empowerment packages + `lairResidentLifeBonus`; manalinks still apply); `applyInteriorDuel` (win → `finalLife` forward + ante to escrow; loss → ante forfeit + world penalty); `resetDungeon` / `clearDungeon` / `colorPrizeRoll` / `lairPrizeRoll`. Worldgen: a **dungeon fixed point per wild region** (named from `dungeons.json`); `advance()` emits `dungeonEntry` thresholds — **lairs are lair-dungeons now** (entering is a choice; the S14 lair test rewritten). Client: telegraph modal (law, stakes, escrow, reset count, resume-aware), interior screen on WorldMapView (cache flags, minion chips, empowerment meter with tier pills, escrow panel, Walk out), interior duels through PlayMatch, victory ceremony, **load resumes mid-dungeon**. Browser-verified end-to-end: telegraph → descend → treasure escrowed → minion fight with the **law live** (both sides' Llanowar Elves in the Emerald Root) → concede → ante + world life + escrow forfeit + reset + eject.
- **Part 4 — content:** `data/world/dungeons.json` (The Pearl Reliquary / Sapphire Deep / Jet Barrow / Ruby Caldera / Emerald Root; guardians at 16/14/15/16/15; the five §6 laws; prizes = Mox + guardian card + colour roll), catalog-validated through both loaders. Lair-dungeon conversion complete (1–2 minions, resident boss + life bonus, no law, same empowerment knob).
- **Part 5 — AI:** `entersChoice` heuristic (pay only above floor 4, only when the untapped source closes a castable gap this main) + **book of shame 17** (never at life ≤ 2 — pinned at 2 and at the floor); solver-aware castability came free via the enumerator; guardian-profile sanity via 2,400 fuzz games (Reya stalls into raises, Titania cracks Wilds into Elementals — both observed in logs/fixtures; no pins needed).
- **Part 6 — measurement:** `pnpm guardian-sim` — kill rates per empowerment tier (15 games × 7 references × 4 tiers each): **Reya 70/75/78/92 · Arcanis 39/52/82/91 · Drana 78/82/93/99 · Drakuseth 71/81/79/97 · Titania 46/49/72/95 (%)**. Clean monotone gradients — dawdling is punished exactly as designed; Drana runs hottest fresh, Arcanis/Titania reward speed most (OQ-15 for the round). **Gold-flow note:** shocks at 45g against S19's 71–116g tour income — a deliberate purchase (~half a tour), consistent with "a real purchase, not an impulse"; no retune proposed.
- **Part 7 (scripted):** the world tests cover the full run (topology invariants + determinism, escrow-not-pocket, law-both-sides, empowerment at 130 steps, a real interior duel applied both ways, walk-out forfeit, victory payout incl. Mox + guardian card + colour roll with provenance, mid-run save round-trip, lair R-treasures + resident bonus); browser run above. **Human half (Chris): pending** — splash a colour with duals/shocks, one Mox dungeon end-to-end, one lair-dungeon, the meter's pressure, the escrow's bite, the telegraph, the laws, the guardian decks.

## Deviations from the brief

1. **The dungeon law applies to minion duels too** — §6 reads as the dungeon's table rule; guardian-only is a one-line change (OQ-16, Chris verdicts in play).
2. **Walk-out is available anywhere** (a button), not only at the entry cell — the design names walk-out an exit choice without pinning a location; standing-at-the-door-only felt like busywork. Flag if wrong.
3. **Renown from dungeons**: lair boss = its tier on clearing; mox guardians and minions = none (OQ-14).
4. **Guardian portraits reuse existing mage portraits** — no renders were briefed for guardians; an art round owes them plates (flagged).
5. **Treasure card rolls can hold duals in any colour's dungeon** (lands are colourless by cost); mox-dungeon card treasures are T2/T3-of-colour otherwise.
6. **world-sim's `--tour all` no longer fights lairs** (thresholds stop the tour; it does not dive). The "lair fights" stat reads 0 — the tour policy predates dungeons; revisit only if a tuning call needs interior sims.
7. **Moxen printings**: the default rule resolved to LEA (Dan Frazier) — no override needed; recorded rather than flagged (the ⚠ was "pending fetch").

## Concerns

1. **Interior ante + a small collection**: an interior LOSS forfeits ante from the real collection while escrowed winnings stay locked — a thin deck can bleed real cards chasing a guardian. Working as designed (the escrow's bite), but if playtest says feel-bad, ADR-079's softening lever (release interior ante through) is a two-line change.
2. **Empowerment pressure vs topology size**: a 12×9 interior with sight 4 means a careful full-loot run lands ~40–70 steps — tier 1 at 60 bites exactly on greedy routes. Feels right in sims; Chris's run decides.
3. **The guardian decks lean on the master profile**; Arcanis at tier 0 (39%) is carried by the deck's tempo shell rather than counters (deliberate, per the pilotability ruling). If the round wants him hotter fresh, life 14 → 16 is the lever, not the list.
4. **`dungeonAsWorldMap` fabricates a WorldMap** (one region, no towns) — any future map-view feature that assumes towns/regions exist should guard (the fabricated heart is off-grid deliberately).
5. **Drana's dungeon deck is the pooled Drana list** (guardian-decks §9), and her card is already pool-R — "her copy rides the same rule" is honoured by the prize table pointing at `drana_kalastria_bloodchief`.
6. **The three seeded-test re-anchors** (WBRUG geometry): pinned templates and region guards — noted so future worldgen changes reach for the same fix instead of seed-hunting.

## Registry entries added/changed

Manifest **Amendment 9** (+ A8-implemented note); **R-057** (backfill), **R-058** (solver), **R-059** (A9), **R-060** (A8), **R-061** (ATTACKS), **R-062** (dungeon duels); pool-registry S20 section + shop-tiers note (36 defs) + printings rows (auto, incl. the 25 overrides); knobs `dungeonGridWidth/Height`, `dungeonEmpowermentTiers` (difficulty-overridden in easy/hard), `priceOverride` column, `beastTierFallback` unchanged; `world-save-v5`; `data/world/dungeons.json` (catalog-validated); art-fetch override table + 429 backoff; open questions **OQ-14/15/16** (+ OQ-12 note: Nighthawk shipped at 17%).

## Test status

**302 passed / 0 skipped-failing (FUZZ_FULL: 302, exit 0)**; typecheck clean. Fixtures +20 (solver 6, A9 5, A8/legends 9 incl. the Drakuseth fizzle case), world +5 (dungeon flow), pins updated (loader **147**, tiers 59/41/11/16, save-v5 chain, lair→threshold rewrite). Fuzz: duals 1,400 · shocks 1,000 · guardians 2,400 — zero errors. Ladder 1,000/cell PASS (Part 0). Browser-verified: telegraph, interior (fog/caches/minions/meter/escrow), law-in-play, loss consequences, resume-on-reload, shock dialog + dual tap chooser (fixtures + controller phases).

## Suggested next

1. **Chris's human half + director round**: the dungeon feel items (OQ-14/15/16, the escrow's bite, the meter, deviations 1–2), dual/shock splash play, guardian-life tuning from the kill tables.
2. **S21 (sieges + retrieval/rumor quests)** per the roadmap: `sieges: []` is reserved, manalinks carry their towns, the lair pattern covers occupation, and the quest template table is ready for two more shapes.
3. **Planner**: guardian plates (art round); the R-economy design now has three live channels (quest rolls, lair treasures, dungeon prizes) pulling on it.

## How to run

```
pnpm test / FUZZ_FULL=1 pnpm test
pnpm fuzz:duals [--shocks | --guardians] [--agents heuristic]
pnpm guardian-sim --games 15            # kill-rate tables per empowerment tier
pnpm ladder --games 1000                # gates (PASS 2026-08-24)
pnpm viewer → /world (mox sites + lair-dungeons on the map; dungeons resume on reload) · /play
pnpm card-manifest                      # price sheet incl. overrides
```
