# Handoff — after Session 19 (2026-08-23)

## State of the world

**Quests are live in Cinquefoil, on a gated agent, over a tiered economy, against the full bestiary grid.** Part 0 first, per ADR-078: the **1,000/cell ladder gates PASS** on the post-pin-16 agent (mirrors within ±2.2 of S16, E +2; floors and vs-random clean), and a **fresh 12-opponent baseline** was filed (`docs/world-sim-s19-baseline.md`) before any content landed — blue on **list C** jumped to 61/45/23 (was 48/26/15). **Shop tiers** (ADR-078): `shopTier 1|2|3|R` lives on every card def (loader-validated; tokens/basics/prizeOnly/test exempt), shelves filter `shopTier ≤ ring`, prices carry `shopTierMultiplier` (1.0/1.5/2.5), **R never stocks** (Demonic Tutor, Mystic Snake). **Bestiary round 2**: the five grid-fillers are encoded, rendered, and at tier in world-sim (A Plague of Rats B1, A Gray Ogre R1, A Savannah Lion W1, A Rumbling Baloth G2, The Faerie Formation U3 — the new card Scryfall-confirmed at `{3}{U}` activation), U's lair reassigned to the Formation, W1 Tactician kept beside the Lion. **The quests slice** (the milestone): courier / card-courier / bounty end-to-end with seeded town boards, deadlines on the step clock, rewards in gold / tier-appropriate cards / **R cards (the first R-acquisition path)** / **manalinks** (`permanentOnBattlefield`, capped per colour, town-tied), all in **world-save-v4** (quests + manalinks + reserved `sieges: []`) with autosave at every consequence. **275/277 tests**, typecheck clean, fuzz over the 16-deck matrix clean. The mid-session round ruled: **Nighthawk/Warband worldLife 10 → 8** (re-simmed: 15% / 36%), plates kept, quest way-marks + completion popup shipped from Chris's test-drive.

## Done this session

- **Part 0 — gates and baseline (blocked everything, ran first):** ADR-078 appended (verified); blue = **list C** in `starters.json` (hard variant now removes an Essence Scatter — Counterspell left the list). **Ladder 1,000/cell: gate/mirror PASS** (A 66.3/72.6, B 70.6/70.1, C 78.1/72.2, D 75.2/79.0, E 64.5/59.0 vs S16's 67.6/74.8, 69.4/69.7, 77.7/72.0, 74.6/78.4, 62.6/57.9), floor rider and vs-random PASS. Fresh baseline (30 seeds × 5, 12-opponent roster): white 72/68/49, **blue 61/45/23**, black 75/37/19, red 82/58/33, green 59/33/29 — the S18 tables are retired for tuning.
- **Part 1 — shop tiers:** `CardDef.shopTier` written to all 96 pre-S19 cards from `card-tier-audit-v2.md` (both ⚑ promotions applied per kickoff: Boggart Brute T2/24g, Rumbling Baloth T2/30g); loader guard (a shoppable card without a tier is a structural error); `shopPoolFor(pool, colour, ring)` filters; `shopTierMultiplier` knob; distribution pinned **53/31/11/2** (with Formation). R sales from the collection price at the T3 factor (interim — see concern 4). Pool-registry gains the shop-tiers section (the column is canonical on the defs; the audit doc is the curation source).
- **Part 2 — bestiary round 2:** **Faerie Formation** encoded ({4}{U} 5/4 Faerie, flying; "{3}{U}: create a 1/1 blue Faerie flying token. Draw a card." — **Scryfall re-verification confirmed {3}{U}**; the Gatherer bare-{3} was a scrape artifact, resolved in the planner's favour); `faerie_1_1_u` token (colors ["U"] per ADR-019) with house-style art wired; ELD #316 Ryan Yee via the default printing rule. Five catalog entries with parley voices (the Formation's verb: *Offer something shiny*), all buyable per kickoff; five decklists in `expansion-decks.ts` (all 16 decks verified 30 cards); **U lair host → the Formation** (top-signature rule); five plates + chips in the house style (the plague a *mass*, the Formation a *throng*), MANIFEST candidates pending verdicts. `fuzz:expansion`: 2,880 random + 1,920 heuristic over 96 pairings, zero errors.
- **Part 3 — quests (the milestone):** `packages/world/src/quests.ts` — offers are a **pure function of (seed, town)** over an authored template table (placeholder text; ≥1 per town, `questsPerTown` default 2; accepting consumes for the game). **Courier** (destination biased far); **card-courier** (ADR-069: a predicate-matching **spare** leaves the collection at acceptance — deck copies refused so decks stay whole; the offer text says the card travels immediately); **bounty** (a spoke signature of the town's tier spawned one ring out, out of sight; **the map mark trails sightings** — fog-honest `bountySeenAt`; bounties never expire). **Deadlines**: `questDeadlineSteps` (220/300/400 baseline vs the ~210-step full tour), ticked in `advance()` — the second clock consumer; expiry fails the quest with no further penalty, as does abandon. **Rewards**: `questGoldByTier` (20/50/100); tier-appropriate cards (`shopTier == tier`), tier-3 rolls upgrading to **R** ~35% of the time; **manalinks** at tier 2+ in the town's colour (cap `manalinkCapPerColor` 1; over-cap converts to gold so the reward is never dead), implemented exactly as the manifest promised — five `manalink_*` defs (`isTokenDef`, `{T}: Add {X}`) injected as `permanentOnBattlefield` modifiers in `prepareDuel`; bounty payouts land on `DuelRecord.questRewards` for the result screen. **world-save-v4**: `quests` (active/completed/taken), `manalinks`, **reserved `sieges: []`** (the blessed field list); v3→v4 defaults; autosave on accept, complete, and expiry. Client: town **quest board** (tier badge, reward line, deadline, card-courier spare picker), rail **Quests panel** (destination/target, sighting status, steps-left urgency colour, abandon) + manalink line, **⚑ last-seen marks** on the map, result-screen bounty line, notices throughout.
- **Part 4 — world-sim, 17-signature roster** (`docs/world-sim-s19.md`): headline white 72/65/48, blue 62/46/24, black 70/39/15, red 84/46/33, green 60/31/28. **All five new decks land at tier**: Rats 64%, Ogre 69%, Lion 73% (T1 band 62–84); Baloth 60% (T2 band); Formation 27% (T3 band). **Formation watch item resolved**: 12 master-piloted games vs slice C — 8 wins, 10 casts, **37 engine activations**; the pilot banks toward the engine, no pin needed. **Gold flow**: a full tour (start 20g, ~10 duels) ends at 71–116 gold; T1 shelf 8–20g, T2 12–36g, T3 30–80g — a tier-3 card is a journey's prize, not an impulse buy; multipliers read sane as baselines, no retune proposed.
- **Part 5 — scripted acceptance (all headless, in `world.test.ts`):** courier accept → offer consumed → mid-quest **v4 save round-trip** → arrival pays → closed "done"; card-courier refusals (no card / wrong colour / deck copy) and the spare leaving; bounty accept → spawn in-region (spoke-verified) → sighting sets the mark → defeat pays on the duel record; deadline expiry mid-walk (event, no reward, **no further penalty**); manalink cap + modifier presence + **a real duel run with the link in play** (the def is engine-real). **Human half (Chris): pending** — courier + bounty, a tiered town (and a wild-town shelf), two new beasts, the Nighthawk chattiness feel, quest-feel verdicts.

## Mid-session director round (ruled)

- **Nighthawk and Warband: worldLife 10 → 8** (Chris — "exercise a capability we'll want eventually": the first per-opponent life tuning, ahead of the full life-total pass he has planned). Re-sim, same seeds: **Nighthawk 11% → 15%** (7-39), **Warband 32% → 36%** (8-14). A real but modest lift — the Nighthawk's lifelink race is only mildly dented by −2 starting life; Chris called it "a fair trade" (the deck should lose to fast starts). If the next round wants more, the levers are another life step or deck surgery; tables in `docs/world-sim-s19.md`'s appendix.
- **Plates ×5 + the faerie token: kept** (MANIFEST updated).
- **Quest UX from Chris's test-drive:** (1) *"no indication of where to go"* — quests now **mark the way**: a courier's destination town gets a ⚑ "{town} · delivery" (shown even through fog — the quest-giver told you the way), a bounty marks its named region's heart "· last marked" until first sighting, then the trailing last-seen mark. (2) *Completion popup* — arrivals now raise a "Quest complete" modal with the flavour line and the reward ("Take it"), instead of a notice line; bounty payouts keep their duel-result line. Both browser-verified.

## Director round 2 (Chris's six notes)

1. **Duress showed nothing** — confirmed and fixed at the engine: the caster-chooses discard only issued its request (the reveal vehicle) with ≥2 legal picks; 0 or 1 matches silently skipped it. Now **the reveal is unconditional** (CR-honest: "target opponent reveals their hand" happens regardless): 0 matches → the full hand + a "Continue (nothing to take)" acknowledgement; 1 match → the hand + the single pick, still shown. Fixtures 5b strengthened + 5c added. **Bonus fix found in the same code**: the AI's caster-chooses pick ranked by its OWN hand map (the opponent's cards resolved to nothing → effectively arbitrary, trending worst); it now reads `request.revealed` and takes their **highest**-mv match. Old replays unaffected (all fixtures green — no checked-in log contained a skipped-request Duress).
2. **Raw card keys** — two distinct sinks: quest reward text now carries the card **name** (resolved at offer time, so world-side award code needs no pool); the **stack panel's target lines** resolved names only for cards that were themselves on the stack — a targeted Fencing Ace fell back to its key. Both fixed.
3. **Card courier: worked nicely** — verdict recorded; the spares-only rule (deviation 3) stands unchallenged.
4. **Collection our-frame scaling** — our frame kept the gallery's 240px width inside the collection's 186px cells (overlap + dwarfing the scans); scoped to 180px in that grid.
5. **Manalinks are real basics now** — a green manalink puts an actual **Forest** onto your battlefield (`permanentOnBattlefield` of the plain basic); the five bespoke `manalink_*` artifact defs were cut the same session they were born (loader pin back to 111). Reward text: "a bonus Forest on your battlefield".
6. **Economy first impressions good**; Chris wants to playtest splashing a second colour once **dual lands** land — planner note: he expects them soon and cheap to compose (they are: `addMana` choice or two basic abilities — Amendment 2 machinery covers the choice form).

## Deviations from the brief

1. **The pool-registry `shopTier` column is a section, not a per-row column** — the canonical value lives on the card defs (loader-validated), the audit doc curates, `pnpm card-manifest` renders the human sheet. A 100-row hand-edit would rot; the guard can't.
2. **R sell price** (a player selling an ante-won R card) uses the T3 multiplier — the audit prices R as "—". Interim, flagged for the R-economy design.
3. **Card-courier takes spares only** (deck copies refused) — ADR-069 says "from your collection"; the stricter read keeps ADR-065's deck-integrity invariant without a refill dance. Escalate if the looser read was intended.
4. **Bounty targets never expire** (the mark keeps roaming) — the brief gave deadlines to couriers ("optional step deadline per quest"); a bounty with a deadline plus a wandering target read as feel-bad. Flag if wrong.
5. **Manalink over-cap converts to its gold value** rather than being undropped — offers are seeded before the cap check can know the future; a dead reward at turn-in felt worse than gold in lieu.
6. **`beastTierFallback` is now dead code for signature rolls** (the grid is complete) — retained as insurance per ADR-078; the OQ-4 `mage` default is formally nodded.

## Concerns

1. **Nighthawk and Warband stand out again on fresh tables** (11% / 32%) — now with the gates re-run and the baseline honest, the numbers are attributable to the decks, not the agent change. The Nighthawk deck's core (4× lifelink-deathtouch-flying + removal) simply plays two tiers up under any pilot. The ruling is this round's business, not mine.
2. **Quest text is placeholder** (three templates per kind) and repeats quickly across towns; the planner's authoring round (with stronghold/boss references when those exist) has a ready seam — templates are data in `quests.ts`, trivially externalizable to `data/world/quests.json` when authored content arrives.
3. **Offers are static per game** (accept consumes; no re-roll). If Chris wants boards to refresh on the shop epoch, it's a one-line change to the offer id scheme (`q_town_i_epoch`), but the manifest's "≥1 per game" is satisfied as is.
4. **The R economy is half-built by design**: R cards now *enter* (tier-3 quest rolls, ante from R-carrying decks — none yet) but only *leave* via selling at the interim T3 price. The dedicated R-acquisition mechanism stays planner-side (ADR-078 banked it).
5. **Bounty spawns don't respect region density** — the mark is an extra roamer on top of the region's target; respawn ignores it (it's not `fixedAt`). Harmless at cap 2 offers/town, noted for the siege session where spawn budgets tighten.
6. **`townOffers` reads clean but is invoked per render** in the town screen (pure + cheap, ~2 offers); if boards grow, memoize in the controller.
7. **Manalink art**: the five defs render in our frame with no asset (fallback "rendered"); if Chris wants Manalink token art, it's the S17 token pattern (five subjects, one render each).

## Registry entries added/changed

Pool registry: S19 section (Formation, faerie token, manalinks ×5) + shop-tiers section + printings row (auto, ELD #316). No rules-registry entries (no engine changes — the quests are world-side; manalinks are the existing modifier). Knobs: `shopTierMultiplier`, `questsPerTown`, `questGoldByTier`, `questDeadlineSteps`, `manalinkCapPerColor` (docs regenerated). `CardDef.shopTier` in the schema; `world-save-v4`. Art-direction §8 (the ADR-078 fog rule). MANIFEST +11 rows (5 plates ×2 crops + token, candidates). ADR-078 appended.

## Test status

Default tier: **275 passed / 2 tier-skipped (277)** — +2 shop tiers, +6 quests/manalinks/save-v4, catalog/lair/spawn/starter/loader pins updated for the grid + list C + 116 loader entries. `fuzz:expansion` 2,880 random + 1,920 heuristic (96 pairings) zero errors. **Ladder 1,000/cell: all gates PASS** (the S19 tuning precondition). Browser-verified: quest board (two couriers, accept), courier delivery ("Quest complete — 20 gold"), bounty accept → rail entry → ⚑ last-seen mark on sighting, tiered shop (a civilized shelf all-T1). FUZZ_FULL pending the close (run before committing the session end if anything changes further).

## Suggested next

1. **This session's remaining half:** the mid-session ruling (Nighthawk/Warband), Chris's human acceptance, plate verdicts, then any ruled deck edits applied and re-simmed in-session.
2. **S20 (per roadmap): sieges** — `sieges: []` is reserved in v4, manalinks carry their granting town for suspension, the lair pattern covers occupation fixed-points.
3. **Planner:** quest-text authoring round (+ boss round feeding stronghold references); the R-acquisition mechanism; Bitterblossom/Hymn "future R candidates" from the audit when the drawer grows.

## How to run

```
pnpm test / FUZZ_FULL=1 pnpm test
pnpm ladder --games 1000                                  # gates (PASS 2026-08-23, post-pin-16 agent)
pnpm world-sim --seeds 30 --starter blue --policy avoid [--no-beasts]
pnpm fuzz:expansion --games 30 [--agents heuristic]       # 16 beast decks × slice decks + mirrors
pnpm card-manifest                                        # regenerate the price sheet
pnpm viewer → /world (quest boards in towns; Quests rail panel; ⚑ bounty marks)
```
