# Handoff — after Session 16 (2026-08-23)

## State of the world

**Roaming visibility is real.** Encounter rolls are gone: every opponent instance is a **roamer** with a map position that moves each player step — toward you within sight, away when your renown says flee, random drift otherwise; contact (same cell, whoever stepped) = parley; **any parley outcome removes the roamer** (Chris's ruling; lair residents leave only when defeated); regions below their density **respawn** one roamer per clock tick, out of sight. The map is 2× linear (80×56 from a `mapScale` knob), towns are per-region by density (every non-wild region ≥1), the player **starts in their colour's town**, and the world UI is a **29×19 viewport** centred on the player with a **minimap**, roamer chips (fleeing marked), and a sight diamond. **`world-save-v3`** carries roamer positions, **multiple decks** (`decks` + `activeDeckName`, picker in the editor: new/duplicate/switch/delete; spares subtract the active deck only), an append-only **provenance** log, `renown`, `starterId`; v1/v2 saves migrate through the chain. **Five authored 30-card mono starters** (`data/world/starters.json`, one-drops in, easy/hard variants) are the new-game choice; slice decks A–E are enemy/ladder infrastructure. Engine: **`mill`** (R-046) and the **creature mana-producer summoning-sickness gate** (R-047); **Llanowar Elves** and **Cathartic Adept** encoded (Scryfall re-verified, art fetched), pool 70→72. Fuzz clean (2,000 starter games + the standard tiers), 225/227 tests, FUZZ_FULL exit 0. **Not done: the radial world generator** — Chris redirected Part 2.1/2.4 mid-session to a five-spoke "concentric pentagram" design; the proposal is in `docs/world-design-round-2-radial.md` and waits on ADR-072 + catalog content (the scaled Voronoi with home-colour start ships as the interim).

## Done this session

- **Part 0 — Docs:** `decisions-append-S16.md` appended (ADR-069..071, verified; ADR-058 intact); manifest **Amendment 3 (mill)** copied verbatim into the Amendments subsection; `docs/knobs.md` regenerated; **R-044/R-045 backfilled** in the rules registry (see Concerns 1); R-046/R-047 added; pool registry Session-16 table + printings rows (Elves LEA #210 Maddocks by the default rule; Adept ALA #34 Critchlow by override, `printings.md` row); `data-model.md` vocabulary row for `mill`.
- **Part 1.1 — `mill` resolver (R-046):** `mill {count, who}` (who incl. `target` with a player target); `EffectContext.mill` moves top-N one card at a time via `moveObject` (ZONE_CHANGE per card), emits **`MILLED {player, objectId, cardId}`** (logged EVENT; play-by-play "You mill X" — graveyards are public), never sets `attemptedDrawFromEmpty`. Fixtures (`s16-scenarios.test.ts`): targeted mill to the opponent's graveyard (two target actions offered, self/opponent); sick Adept offers nothing; empty library mills nothing and nobody loses; **mill-to-empty then the DRAW loses (DECKED)**; **a milled Pelakka Wurm fires no DIES** (library→graveyard asserted on the bus, no DIES fact, no draw); replay byte-identical through a mill-happy game.
- **Part 1.2 — creature mana producers (R-047, CR 302.6/602.5g):** `producibleSymbols` and `tapForMana` refuse {T} mana abilities on a sick creature without haste (the non-mana activated path already had the gate since S1); canPay/autoPay therefore exclude it. Fixtures: rested Elves + two Forests casts Courser (Elves taps); sick Elves → no `tapForMana`, Courser not castable, Bears still is; Elves cast T1 is sick that turn and rested after the next untap; Doom Blade kills it like any creature.
- **Part 1.3 — cards + agent:** both defs Scryfall-verified (`{G} Elf Druid 1/1 "{T}: Add {G}."`; `{U} Human Wizard 1/1 "{T}: Target player mills a card."`); loader 72. AI: Elves is priced as a body (mv 1 → 1.0) and is cast early (25 of 67 casts on T1–2 over 60 games); **the Adept milled ITSELF ~half the time** under the flat 0.1 valuation → target-aware mill valuation (opponent +0.25/card, more when their library ≤5 or empties; self −1.5/card) + **book of shame 10** (mill-them > pass > mill-me); re-measured: self-mills 48 → 0 of ~90 activations. `view-sim`'s clone now copies `librarySizes` (mill mutates it).
- **Part 2.1 — scale + viewport:** knobs `mapScale` (2.0 ⇒ 80×56), `townSpacingMin` (8), `townsPer100Cells` ({civilized 0.25, approach 0.12, wild 0}, floor 1 for non-wild); `WorldMapView` renders a 29×19-cell window centred on the player (cells culled to the window), **minimap** (whole map, region runs, towns/lairs/roamers, viewport rectangle; click = preview a path), `CELL` 18→24. Region labels draw when their heart is in view. `shopRefreshSteps` 50→100 (clock retune for the bigger map).
- **Part 2.2 — roamers (ADR-071):** `OpponentInstance` gains `at`, `gone`/`goneReason`, `moveDebt` (`defeated` retired); `spawnRoamers` (in-region, passable, never town/fixed, reachable preferred; also used by the v2→v3 migration); `advance()` per step: player moves → town is safe → lair certain → **stepped-onto contact** → `tickRoamers` (speed debt per `roamerSpeed[tier]`; toward if `manhattan ≤ sightRadius` and not fleeing, away if fleeing (holds still rather than stepping into you), drift = stay 50% / random legal neighbour; **region-bound**, never onto towns) → `respawnRoamers` → **reached contact**. Player sight = `sightRadius − roughSightPenalty × rough cells on the Bresenham line` (`effectiveSight`/`playerSees`/`visibleRoamers`); roamers' own sight is the plain radius (the ambush). Encounters carry `fleeing` and `contact: stepped|reached|lair` (notices: "catches up with you" / "You run down X — they were fleeing" / "guards this place" / "blocks your way"). Knobs: `roamerDensityPer100Cells` ({1.0, 1.5, 2.0}, min 1/region), `roamerRespawnSteps` ({40, 30, 20}; 0 disables), `roamerSpeed` ({1,1,1} per tier — the hook for roads/boots/slowing terrain), `sightRadius` 6, `roughSightPenalty` 2. `encounterRatePerStep` retired (difficulty bundles moved to density deltas).
- **Part 2.3 — renown + fleeing:** `player.renown` (Σ tier of defeated; losses subtract nothing), `renownFleeFactor` {4, 8, 16}: flee when `tier × factor < renown`, evaluated every step; pursuable (you step onto it; `fleeing: true` on the encounter).
- **Part 2.4 — home start + uniform towns + invariants:** the generator guarantees **colour coverage** (every colour has a civilized-or-approach region — swaps a duplicate template when the roll misses), ≥1 town per non-wild region, start = the home colour's town (civilized preferred), and (interim, Q4) the **home region rolls the civilized tier table** even when its tier is approach (Red/Black today). Generator fuzz 200 seeds: colour coverage, town-per-region, home start, roamer spawn legality (in-region/passable/not-town), density targets met, determinism; plus sight/pursuit/respawn determinism across save/load.
- **Part 2.5 — `world-save-v3`:** roamer fields, `decks`/`activeDeckName` (`player.activeDeck` and `deckName` retired; `activeDeck(world)` helper), `provenance: {cardId, source: starter|ante|shop|reward, step}[]` (written from S16 on: starter/spares at step 0, ante wins, shop buys), `player.renown`, `player.starterId`. Migration v1→v2→v3 (decks from the old active deck under the old name; `gone` from `defeated`; positions seeded from the world seed — deterministic; `starterId` from the basic land). Deck API: `createDeck` (30 basics — legal by construction, ADR-065), `duplicateDeck`, `renameDeck`, `deleteDeck` (never the active/last), `switchDeck` (re-checks legality + ownership — a non-active deck can drift to copies you no longer own since spares count the active deck only). Controller: `deckNames/deckNew/deckDuplicate/deckSwitch/deckDelete` + the editor header picker (dropdown + new/duplicate/delete via `prompt`).
- **Part 3 — starters:** `data/world/starters.json` (catalog-validated: five ids, 30 cards, basicLand in list, variants ≥30; the loader/bridge read it); `newWorld({starter})`; `starterDecklist(starter, difficulty)`: easy +1 removal +1 three-drop (32 cards — white has no three-mana creature in the pool, Glorious Anthem stands in), hard swaps the rare-adjacent card for a basic (stays 30). `StarterTemplate.archetype` drives the pilot profile; opponents' `deck` may now be a slice key **or `starter:<id>`** (`enemyDeck()` resolves decklist + archetype). world-sim rewritten (`--starter`, `--policy fight-all|avoid`, `--tier1-deck starter`, `--tier1-life N`); **`pnpm fuzz:starters`** (starters × slice decks + mirrors) — 1,200 random + 800 heuristic games, zero errors.
- **Part 4 — acceptance (scripted):** roamer pursuit → contact → parley (world + controller tests); flee at high renown with pursuit (retreats, never steps into you; cornered → caught with `fleeing: true`); sight radius honoured incl. the rough penalty and the roamer-still-pursues asymmetry; home-region start per colour; v2 save migrates and plays (controller test + v1 chain); deck picker create/duplicate/switch/delete and the active deck duels; browser-verified: new game (green, seed 16) → roamer chip in sight → it shadows me → contact → parley → flee-failed → duel → removed (`gone: lost`); editor picker ops. **Human half (Chris): pending** — wander the new map, dodge one, pursue a fleeing one, Elves on T1; file the felt-wrong list (map scale, sight radius, roamer speed above all).

## Deviations from the brief

1. **Part 2.1/2.4 redirected by Chris mid-session** to the radial ("concentric pentagrams") world: proposal written (`docs/world-design-round-2-radial.md`, §7 lists the asks — ADR-072, 15 region names + town names, 5 stronghold names, a ruling on reserving `explored` for fog of war in v3). Not implemented this session (no ADR/content back yet); the brief's scaled Voronoi + home-colour start + uniform towns ship as the interim. **Recommend S17 opens with it** — the generator change is small (radial heart placement + ring tiers + strongholds + roads) and everything else is geometry-agnostic.
2. **Home region rolls the civilized tier table** (Q4 interim): Red/Black starters begin in approach regions; without this a Red player opens against tier-2s. Logged as interim; the radial world makes it moot (every colour gets a civilized region).
3. **Hard variant swaps the rare-adjacent card for a basic** (the doc's "removes" would make 29 cards); **easy adds** per the doc (32 cards). Concrete picks (planner adjusts): W +Pacifism +Glorious Anthem / −Swords; U +Boomerang +Man-o'-War / −Counterspell; B +Doom Blade +Phyrexian Rager / −Doom Blade; R +Shock +Boggart Brute / −Lightning Bolt; G +Prey Upon +Centaur Courser / −Prey Upon.
4. **Any parley outcome removes the roamer** (Chris's ruling replaced the brief's implicit evading), with **respawn** (`roamerRespawnSteps`) added so regions don't empty — the first real clock consumer (manifest §5). Lair residents stay until defeated.
5. **`roamerSpeed` per tier** (Chris: keep speed tunable) instead of a hard 1; shipped at 1 per the brief. **`roughSightPenalty` applies to the player's sight only** (roamers still pursue from behind crags — the ambush).
6. **Provenance is written from S16** (starter/ante/shop) rather than left empty; shape = append-only log (Chris OK'd).
7. **Opponent `deck` may reference a starter** (`starter:<id>`) — added for the tier-1 measurement; no catalog content uses it yet.
8. **R-044/R-045 backfilled** into the rules registry (claimed by the S15 handoff, never landed).
9. **A pre-existing `.tsx` type error** (editor filter, not in `pnpm typecheck`'s include) fixed in passing; the UI package still isn't typechecked by `pnpm typecheck` — suggest adding `tsc -p packages/ui/tsconfig.json --noEmit` to the script.

## Concerns

1. **The S15 registry edit never landed** — the handoff said R-044/R-045 were added; `rules-registry.md` was last touched in S12. Backfilled from the handoff text. Principle 11 should apply to handoff claims: grep the registry before writing "entries added".
2. **Starters miss the targets — honestly.** 30 seeds, journeyman pilot, `avoid` policy, tour = all towns + the lair, every contact fought, tier-1/2/3 win % (deaths/30):
   | starter | as shipped | tier-1 enemies on starters | tier-1 life 5 |
   |---|---|---|---|
   | White "Dawn Levy" | **62 / 45 / 27** (7) | 72 / 44 / 27 (7) | **77 / 48** / 24 (6) |
   | Blue "Tidal Grimoire" | **25 / 15 / 5** (19) | 25 / 10 / 6 (20) | 33 / 14 / 4 (18) |
   | Black "Pallid Court" | **62 / 40 / 21** (9) | 58 / 34 / 23 (10) | **72 / 40** / 20 (6) |
   | Red "Ember Warband" | **70 / 31 / 17** (7) | 55 / 28 / 22 (11) | **86** / 31 / 16 (5) |
   | Green "Verdant Trail" | **44 / 18 / 8** (15) | 41 / 21 / 11 (17) | 54 / 21 / 7 (13) |
   (fight-all is 3–8 points worse across the board; S15 baselines for slice decks as starters: A 76/55/34, C 50/38/33, D 89/79/74.) Findings: **enemy deck quality is not the lever** (tier-1 enemies on starter decks barely move anything); **tier-1 enemy life is** (8→5 lifts white/black/red past 70%, as in S15); **green and blue miss under every condition** — blue in particular (25% tier 1; its mirrors deck out). Green's list is plausible on paper, so I suspect the journeyman pilot (pump/fight timing) more than the cards; blue is a control/tempo shell the AI has never piloted well (deck E was the weakest gate deck). Planner's call: tier-1 `worldLife` 5 in the catalog, AI work on control/tempo, or list edits.
3. **Equal-speed pursuit can't close from behind** (Manhattan geometry — see implementer notes): a roamer that reaches distance 1 shadows you indefinitely while you walk away/sideways; it catches you only when you step toward it, stop, or walk into one ahead. world-sim: ~35% of contacts are "reached", ~65% "stepped". Chris's felt-wrong list should rule on `roamerSpeed` (1.25 for tier 2–3 hunters would let them catch up every fourth step) — the knob exists, the default is the brief's 1.
4. **Encounter economics** (steps per fight by region tier, 30 seeds): fight-all ≈ 30/17–20/9–10, avoid ≈ 40/24–32/11–16 (civilized/approach/wild). S14's roll-based baseline was 3.5/8.5/17.3 per 100 steps (≈ 29/12/6 steps per fight); densities are knobs — approach/wild are quieter now, civilized similar. Fleeing roamers are seen (10–90 per 30 tours) but rarely caught (0–4): renown {4,8,16} flips tier-1s only after ~5 wins; tune from Chris's play.
5. **Tours die a lot** (6–26 of 30 even avoiding): the bigger map means ~12 fights per tour and losses compound through world life. That is the fight-everything pilot, not a rule; but a human who fights half of what they meet will still feel it. Watch with the felt-wrong list.
6. **Mill-to-deck-out is now a real clock in 30-card decks** (blue mirrors: 16/20 DECKED at journeyman). The Adept is a starter card; if that reads badly in play, the cheap fix is a starter list edit, not engine work.
7. **Fog of war / `explored` field** (Chris's direction, proposal §6) needs a ruling before S17 builds POIs: reserve `explored` in v3 now (I'd do it as a no-op field) or take a v4 later.
8. **The UI package isn't in `pnpm typecheck`** (Deviation 9).

## Registry entries added/changed

R-044, R-045 (backfilled), **R-046** (mill, 701.13), **R-047** (summoning sickness on creature mana abilities, 302.6/602.5g); pool registry Session-16 table (Elves, Adept) + starters note + two printings rows; manifest §3 Amendment 3; ADR-069/070/071 appended; knobs: `mapScale`, `townSpacingMin`, `townsPer100Cells`, `roamerDensityPer100Cells`, `roamerRespawnSteps`, `roamerSpeed`, `sightRadius`, `roughSightPenalty`, `renownFleeFactor` (+`shopRefreshSteps` 100; `encounterRatePerStep` retired); data-model `mill` row; `printings.md` Adept row; book of shame 10.

## Test status

Default tier: **225 passed / 2 tier-skipped (227)** — +9 engine fixtures (s16), +1 book of shame (10), +5 world (starters, v3 chain, roamers ×6, multiple decks), +2 controller (v2/v1 migration, deck picker, roamer map), loader 72. **FUZZ_FULL: 227 passed, exit 0.** Typecheck clean (+ `packages/ui` clean). **Fuzz-before-fixtures:** `pnpm fuzz:starters` 1,200 random + 800 heuristic (40 pairings), zero errors; the standard random fuzz tiers in `pnpm test`. Ladder gates **not re-run**: slice decks unchanged; the only agent change (mill valuation) touches no slice card. Browser-verified: start screen (five starters), viewport + minimap + sight diamond + roamer chip, pursuit → parley → duel → removal, editor picker.

## Suggested next

1. **S17 opens with the radial generator** (ADR-072 + 15 region names + 5 stronghold names from the planner; `docs/world-design-round-2-radial.md`) + roads with speed-by-terrain + the fog-of-war ruling — then bestiary/anchors as planned.
2. Chris's felt-wrong round on the roaming map (scale, sight, roamer speed, densities) — the knobs are all there.
3. The starters question (Concern 2): tier-1 enemy life, green/blue lists, or AI work on control/tempo piloting.
4. Pending verdict lists carried forward: S13 world-loop felt-wrong, S14 editor usability, portrait verdicts.

## How to run

```
pnpm test / FUZZ_FULL=1 pnpm test
pnpm fuzz:starters --games 50 [--agents heuristic]       # starters × slice decks + mirrors
pnpm world-sim --starter green --policy avoid             # per-starter tour baselines (fight-all | avoid)
pnpm world-sim --starter red --tier1-life 5               # measurement flags: --tier1-life N, --tier1-deck starter
pnpm ladder --games 1000                                  # gates (slice decks; unchanged this session)
pnpm knobs:doc                                            # regenerate docs/knobs.md
pnpm viewer → /world                                      # new game picks a starter colour; dev handle __wc
```
