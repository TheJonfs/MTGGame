# Handoff — after Session 13 (2026-08-21)

## State of the world

**The overworld loop is playable at `/world`.** New game (colour → starter deck, difficulty bundle, seed, name) → an ink-and-wash map of six seeded regions → click-to-walk with a dotted path preview and a live step counter → encounters interrupt the walk with a reveal (portrait, name, tier badge, colour pips, their world life) → parley with real numbers (Fight / Flee with odds and the ADR-063 compounding telegraph / Buy off with the price, greyed when unaffordable) → the duel runs in the existing play client through the S12 `custom` path at world life with the enemy named and portraited → a result screen narrates the consequences (stake cards shown by frame, gold and world-life deltas, the basic-land refill, defeat mark) → towns overlay the map with a seeded shop (buy only) and a read-only collection browser → autosave on town entry, after every duel and purchase, manual save, download/upload, Continue from the start screen → game over at the floor with "watch the fatal duel". The S12 core is untouched and unversioned (**no `world-save-v2` was needed**). 183 tests default tier, FUZZ_FULL green. What remains of the brief is its human half: **Chris plays**, plus the two director rounds the brief budgets.

## Done this session

- **Part 1 — map (`WorldMap.tsx`, pure SVG):** flat wash per region tier tinted by region colour (civilized calm, approach saturated, wild dark), ink borders wherever neighbouring cells differ in region, diagonal hatching on rough terrain, region names at hearts (clamped inside the map), town glyphs (house on a parchment disc) with hover labels, player as a portrait chip (brass ring), dotted ink path preview with a brass target ring, encounter marker (enemy portrait chip, red ring) at the encounter cell. Click = preview (BFS path, step count in the prompt); click the target again = walk. Walking advances one cell per 140 ms (each cell = one step = one clock tick), the chrome's step counter ticks live, encounters interrupt at the cell.
- **Part 2 — parley (`ParleyPanel`):** portrait, name, tier badge (I / II gold / III red), colour-identity pips, deck + difficulty + world life, stake count and your gold; **Fight** ("duel at your world life N; win: their stake + gold; lose: your stake and 1 world life"), **Flee (odds %)** with the ADR-063 telegraph verbatim — *"Forfeit your stake either way; if caught you fight and stake again"* — and **Buy off (price)**, disabled with the reason when unaffordable. Refusals show inline. A failed flee launches the forced duel (stakes compound, as ruled). The **duel** runs in `PlayMatch` on a `MatchController` built from the S12 `PreparedDuel` (`custom`: named decklists, enemy difficulty/archetype/portrait, world-life `startingLife`, `ante`, the enemy `startingLife` modifier); the play client's status blocks now show **the enemy's name and portrait** (`MatchController.names/portraits`, `StatusBlock name/portraitSrc`). **Result screen:** Victory/Defeat/Draw — enemy name; "You claim their stake" / "Your stake is lost" with the cards as frames; the basic-land refill sentence; gold and world-life before → after with deltas ("fatal" at 0); "defeated — this region grows quieter"; Continue / Watch replay (autosaves before handing the log to the viewer).
- **Part 3 — town / shop / collection:** the one headless piece S12 left — `packages/world/src/shop.ts`: stock is a pure function of **(world seed, town index, epoch)** with epoch = `floor(steps / shopRefreshSteps)` — no shop state in the save, stock refreshes with the clock; drawn from the pool by region colour (mono/colourless within the colour; never basics, never tokens); **price = round(shopPriceMultiplier × shopBasePrice × (1 + mana value))**; slice rule: stock does not deplete (a repeat buy costs gold only). Two registry-first knobs: `shopRefreshSteps` (50), `shopBasePrice` (4) — in the generated doc. Town screen overlays the map ("clock-free: deliberation costs nothing here"), shows gold, the stock as mini frames with price buttons (disabled when unaffordable), purchase notices, Collection / Save / Leave. **Collection browser** = gallery grid filtered to owned cards with counts and "in deck ×N" (read-only; colour/land filters; Back).
- **Part 4 — shell:** start screen (colour → starter deck A–E by colour identity, easy/standard/hard — the untuned ones say so, name, seed, **Continue** when an autosave exists, load a save file; links to /play, viewer, gallery); chrome (name · region, world life, gold, steps, collection / save / download, seed · difficulty); rail (journey tally, regions by tier, recent duels with per-duel **replay** links); autosave on new game, town entry, after every duel, after every purchase, and before any viewer hand-off; manual save; download (`world-<seed>.json`) / upload; game-over screen ("Your journey ends" with steps/duels/gold/cards, **Watch the fatal duel**, New journey). The viewer's landing page links **Walk the world** first.
- **Part 5 — acceptance (scripted half):** `world-controller.test.ts` drives the controller through the click path: new game → preview → walk to a town (encounters off via the event layer) → town autosave → `saveText`/`loadText` identical world; shop stock → buy (gold → collection) → collection open/close → leave; forced encounter (event layer rate 1) → buy-off or refusal by tier → flee → fled or caught-and-fought through the real play client; fight with world life 1 → the duel in the play client (`custom`: starting life 1, ante ≥1, enemy name + portrait) driven by the S10 scripted human → result screen with before/after → win returns to the map, loss → world life 0 → **game over**, and the autosave reloads onto the game-over screen. **Browser-verified** at seed 7: start, map, path preview to Whitewell, Sister Oriel (tier I) after two steps, parley, fight → play client at 10 life with libraries 32 (= 40 − 7 − 1 ante: Goblin Chieftain vs Curiosity), Control Magic and the stack stop inside a world duel, defeat narrated (stake lost, refill, 10→9), map with the journey tally, town overlay with 8 green-region cards, a purchase (20→12, "Bought Giant Growth for 8 gold"), collection with correct counts, Continue from autosave; no console errors throughout.
- **Part 4's portrait verdicts**: the five candidates render in situ (parley reveal, encounter marker, play-client status block) — **Chris's verdict pending** (MANIFEST rows say so).

## Director round 1 (Chris's first look, same day)

1. *"Great to see it in action."*
2. **Printed card by default** in the world screens (shop, collection, duel result), each with an "our frame / printed card" toggle; custom cards fall back to our frame automatically. The play board's hand stays our frame (ADR-043) — **ruling wanted** if Chris meant that too.
3. **Ante visibility without bending the contract:** the engine picks the stake *after the shuffle* (CR-ante's random pick), so before the duel the world can only say "N random nonland cards each" (it does); the moment the duel starts the stake is known — the play rail now shows a **Stakes** panel ("You: Goblin Chieftain · Sister Oriel: Curiosity") in any ante game. Pre-choosing the card would need a new engine rule; not done.
4. **Blaze for X=0 (apprentice, turn 1):** softmax noise — X=0 scored 0.25 below passing and apprentice runs at 1.2. Now an X-cost spell/ability at X=0 scores −∞ (never a play, like a bare `tapForMana`). Book-of-shame entry 8. Journeyman mirrors 200/cell: 1361 → **1373/2000 (+0.6)** — a small gain, not a cost.
5. **Combat zoomed by:** with nothing castable your combat windows were lone passes, so the lane existed for milliseconds. New default-on stop **"attacks and blocks are declared"** (menu): pauses once when attackers are declared against you ("Opponent attacks with Boggart Brute (Bonesplitter).") and once when blocks are declared against your attack ("Opponent blocks: X ← Y." / "No blocks — your attack goes through."), via the request path or the `onLonePass` seam; fast-forward skips it. Your own attackers now sit visibly in the red zone at that pause. Acceptance test asserts the pause fires.
6. **Enemy life jumping 10→8 during mulligans:** `startingLife` modifiers now apply at state creation, before setup; every other modifier still applies after setup (zones must exist). Still "initialization" per ADR-002 — **engine-ordering change, flagged for ratification**; the custom-path test asserts 8 at the very first pause.

## Director round 2 (same day)

1. **Shop/result sizing:** printed scans and our frame now share one 130×182 card slot (full frame scaled, scan at the same width); the collection's scans match our 180px frame.
2. **Auto-win (dev):** `MatchController.autoWin()` ends the duel as an opponent concession in your favour (the world treats it as a win — stake claimed); the menu shows it **only in dev builds** (`import.meta.env.DEV`), so production never has it.
3. **Stakes hover → inspector:** the Stakes panel's card names are hover targets; the ante objects are real state objects, so the inspector shows the card.
4. **Director backlog for the next design round (M6b+), Chris's words banked verbatim:** larger regions; start the player in their starting deck's home region; towns more uniformly distributed; starting decks as 30-card mono-coloured decks; weaker starting enemies that *flee from you as you grow stronger* so low-level mobs needn't be fought late unless you want to. All are generator/catalog/encounter-table content — none touches the save format except possibly "home region" (a region→colour binding the catalog already carries).

## Deviations from the brief

1. **Shop stock does not deplete** (slice). Depletion and selling need per-shop state — a `shops` field in a versioned save — so they're M6b (Concern 2). Epoch-based refresh is my proposal for "refresh rules open."
2. **Two knobs added** (`shopRefreshSteps`, `shopBasePrice`) registry-first, per the brief's rule.
3. **The walk is interrupted at the encounter cell and the remaining path is discarded** (you re-click after parley) — simplest and honest; a "resume path" button is an easy rider if it feels like friction.
4. **Autosave is broader than "town entry + manual"**: also after every duel and purchase and before viewer hand-offs — consequences should never be lost to a reload (a dev-server HMR reset showed exactly that failure mode).
5. **Play-client additions for world duels** (`names`, `portraits` on `MatchController`; `StatusBlock` props; the Stakes panel) — the S12 `custom` path was sufficient for the game; these are presentation only.
6. **Engine touched (director round 1, item 6):** `Game.run` applies `startingLife` modifiers before `setup()`; all other modifiers after, as before. Initialization-only semantics unchanged; ratify.
7. **Agents touched (director round 1, item 4):** X=0 on X-cost spells/abilities is never a play — correctness (Chris's playtest), measured +0.6 on journeyman mirrors.

## Concerns

1. **Knob defaults felt right in my own walkthrough but are unplayed by Chris** — the brief's primary output. My guess at what he'll flag: `encounterRatePerStep` civilized 0.04 gives an encounter roughly every 25 steps — a town-to-town walk (~20–25 steps) meets about one enemy; approach 0.08 / wild 0.12 are untested by a human; `fleeOddsByTier` 60/50/40; `startingGold` 20 buys exactly one tier-1 pass. Record his directions as director-round entries.
2. **What the shop wants for M6b:** depletion (per-shop remaining counts), sell (half price?), and "restock on refresh" — all want a `shops: Record<townIndex, {epoch, sold: Record<cardId, n>}>` field → `world-save-v2` with a migration (new field defaulting empty). Also a "buy for deck" flow once the editor exists.
3. **Map-rendering fights:** none serious. SVG rects per cell (1,120) render fine; region labels needed clamping; the wash palette is a first guess (tier × colour table in `WorldMap.tsx`) and will draw the art round. Towns' hover labels are plain parchment tags. Not done: roads, coastlines, terrain variety beyond hatching — content for M6b's map pass.
4. **World-state fields the UI wished for:** (a) a per-town `visits` count (for "first time here" text and shop rules); (b) `lastTownIndex` in the save (the controller keeps it in memory for the collection Back button; a reload lands you on the map — harmless); (c) an encounter-in-progress record (a reload mid-parley loses the encounter — currently the autosave is pre-encounter, which is the forgiving direction). All would be `v2` fields; none blocks play.
5. **HMR resets the world during development** (the controller lives in a `useMemo`): harmless for players (no HMR), but during director rounds I should edit between walkthroughs, not mid-duel.
6. **The enemy's world life and the stake are visible only in parley and the result** — during the duel the play client shows life totals but nothing says "ante: Goblin Chieftain". A small rail line ("Stakes: X vs Y") in world duels would help the sting land; trivial if Chris wants it.
7. **`gameOver` UX:** the result screen's Continue reads "…" when fatal — it should probably read "Your journey ends"; cosmetic.

## Registry entries added/changed

None (no rules, no cards). Knobs: `shopRefreshSteps`, `shopBasePrice` (docs/knobs.md regenerated, test-pinned).

## Test status

Default tier: **184 passing / 2 tier-skipped, ~11s** (adds the shop test in `world`, the 4-test `WorldController` acceptance in `ui`, and book-of-shame entry 8 — the book is 8). FUZZ_FULL: **186 passing, exit 0, ~97s**. Director round 1 browser-verified: printed shop, enemy name/portrait + 8-vs-9 from the first view, Stakes panel, and the combat pause with attackers in the red zone. Typecheck clean. Browser-verified per Part 5 above.

## Suggested next

1. **Chris plays** (the brief's human half): a new world, several encounters, one deliberate loss, a purchase, save/quit/resume, a game over if the dice allow — his felt-wrong list + portrait verdicts drive the two director rounds.
2. Then **M6b**: deck editor (the collection model and `deckLegal` are ready for it), shop depletion/sell (`v2`), clock consumers, dungeons/strongholds (the generator's fixed-point API is waiting), boss content. The tutor card half-session slots before the editor.

## How to run

```
pnpm viewer                 # → /world (the loop), /play, /, /gallery
pnpm test                   # default tier ~11s
FUZZ_FULL=1 pnpm test       # full tier, exits 0
pnpm knobs:doc              # regenerate docs/knobs.md (test-pinned)
```
